import { Post } from "../utils/post";

export const oldClusterPrompt = `Ты — профессиональный классификатор новостей и контента. На входе ты получаешь массив Telegram-постов, собранных за конкретный временной промежуток из множества каналов на любые темы.

---

📥 Входные данные:
JSON-массив объектов с постами, где каждый объект содержит:
- 'id': уникальный идентификатор Telegram-поста (целое число, например: 12345)
- 'text': текст поста (может быть новостью, рекламой, флудом и т.д.)

---

🎯 Твоя задача:
1. Прочитать каждый пост.
2. Определи, относится ли пост к одной из **следующих тем** (тематика указана строго):

   - 'Политика'
   - 'Экономика'
   - 'Крипта'
   - 'Технологии'
   - 'Отношения и психология'
   - 'Наука и космос'
   - 'AI и нейросети'

3. Если пост:
   - **информативный, фактологический и осмысленный** (событие, заявление, тренд, цифра, явление) → отнеси его к **одной подходящей теме**
   - **пустой, флуд, реклама, эмоции, спам, юмор** → **не включай его в результат**

4. Сгруппируй отобранные посты в **один JSON-объект**, где:
   - Ключи — это названия тем из списка выше.
   - Значения — массив 'id' постов, отнесённых к теме.
   - Один пост может попасть **только в одну тему**.

---

📤 Формат выходного JSON:
{
  "Политика": [
    12345,
    12346
  ],
  "Экономика": [
    22345
  ],
  "Крипта": [],
  "AI и нейросети": [
    99876
  ]
}

---

Если по какой-либо теме не было ни одного поста — просто укажи пустой массив.

---

📎 Примечания:
 • Не изменяй id постов.
 • Не добавляй никакие новые темы.
 • Не дублируй посты в разные категории.
 • Максимальная строгость к фильтрации: лучше меньше, но точнее.`;

export const checkPrompt = `Ты профессиональный модерадор контента новостных каналов на разные тематики.
---
На вход ты получаешь JSON объект с новостями, отнесенными к темам, где ключ это тема, а значение - массив с новостями, содержащими:
- 'id': уникальный идентификатор (целое число, например: 12345)
- 'text': текст (может быть новостью, рекламой, флудом и т.д.)

Твоя задача:
1. Прочитать все новости по теме.
2. Определить является ли текст новостью (содержит информативный, фактологический и осмысленный текст; не содержит флуд, реклама, эмоции, спам, юмор).
3. Определить относится ли новость предложенной теме.
4. Определить есть ли среди одной темы несколько одинаковых новостей (они могут быть в разных формулировках и с разными деталями, но об одном событии).

Ответ верни в формате JSON,
где notNews - задача 2: массив с идентификаторами новостей (0 или больше), которые НЕ являются новостью;
wrongTopic - задача 3: массив с идентификаторами новостей (0 или больше), отнесенных к неправильной теме;
dublicates - задача 4: массив с массивами (0 или больше), содержащими идентификаторы новостей (2 или больше) об одном событии, где каждый вложенный массив относится к своему событию

📤 Пример выходного JSON:
{
  notNews: [11112],
  wrongTopic: [12345, 12346],
  dublicates: [[23590, 12312, 32132, 123221], [32189, 19862]]
}

📎 Примечания:
 • Не изменяй id новостей.
 • Если новость нельзя отнести ни к одной теме, то помещай ее в массив "notNews", вместо "wrongTopic".
 • Максимальная строгость к фильтрации: лучше меньше, но точнее.`;

export type CheckResult = {
  notNews: Array<number>;
  wrongTopic: Array<number>;
  dublicates: Array<Array<number>>;
}

export function getRetryClusterPrompt(checkResult: CheckResult): string {
  let prompt = 'В твоем ответе найдены ошибки:';
  // if (checkResult.notNews.length) {
  //   prompt += `Идентификаторы постов, которые не являются новостью - [${checkResult.notNews.join(',')}];`;
  // }
  if (checkResult.wrongTopic.length) {
    prompt += `Идентификаторы постов, которые отнесены в неправильную тему - [${checkResult.wrongTopic.join(',')}];`;
  }
  // if (checkResult.dublicates.length) {
  //   prompt += `Идентификаторы постов одной новости в разных формулировках, где каждый вложенный массив это отдельная новость - [${JSON.stringify(checkResult.dublicates)}];`;
  // }

  return prompt + ' Проверь и исправь замечания. Ответ верни в том же формате, что и раньше.';
}